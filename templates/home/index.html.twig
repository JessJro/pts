{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

{# --Présentation-- #}
<div class="wrap">

<section>
<div class="container-presentation">
    {% for item in presentation %}
    <div>
        <h2>{{ item.title1}}</h2>

        <h3><i>{{ item.headline1}}</i></h3>
        <img src="/uploads/files/{{item.image1}}" alt="">
        <p>{{ item.content1}}</p>
    </div>
    <div>

        <h2>{{ item.title2}}</h2>
        <h3><i>{{ item.headline2}}</i></h3>
        <img src="/uploads/files/{{item.image2}}" alt="">
        <p>{{ item.content2}}</p>
    </div>
    {% endfor %}
</div>

</section>


<section>
    <div class="container mt-4">
        <div class="row">
      
          <div class="col"></div>
          <div class="col-md-6">
     
            <div class="card shadow mb-5 p-5">
                
             
              <form method="post" data-form-title="CONTACT US"> <h2 class="mt-3"> Nous contacter: </h2>
                 {{form(form)}}
              </form>
            </div> 
        </div>
          </div>
        </div>
      </div>
    </div>
  </section>


{% endblock %}
